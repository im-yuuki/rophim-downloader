# rophim-downloader

How to start browser (via terminal / powershell)
& "C:\Program Files\BraveSoftware\Brave-Browser\Application\brave.exe" --remote-debugging-port=9222 --user-data-dir="C:\brave-debug"
or gpt about how to connect "Your Browser" into playwright

# Install
erm... tải playwright trước
```bash
pip install pytest-playwright
playwright install
```

### (16/12) This not works btw